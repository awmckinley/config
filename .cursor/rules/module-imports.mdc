---
description: Create a default.nix that imports all other Nix files in this directory
globs: modules/*/default.nix
alwaysApply: false
---
1. Check directory contents
2. Create/update default.nix with `_: {` format
3. Import all .nix files (except default.nix) in alphabetical order
4. When a directory has no .nix files to import, use `_: {}` rather than declaring empty imports
